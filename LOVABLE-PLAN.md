# پلن کار با Lovable — پروژه تستی «دامداری مهر»

> این سند برای اولین پروژه‌ی Lovable نوشته شده. هدفش دو چیزه:
> ۱) مطمئن شدن از اینکه اتصال Lovable واقعاً کار می‌کنه،
> ۲) داشتن یک روال تکرارشدنی و دقیق برای پروژه‌های جدی بعدی.

---

## ۱. وضعیت اتصال

| مورد | مقدار |
|---|---|
| حساب | `mohamanasri27@gmail.com` (MOHAMAD nasri) |
| Workspace | `MOHAMAD's Lovable` |
| نقش | owner |
| پلن | Free |
| اتصال MCP | ✅ فعال |

ابزارهایی که از داخل چت در دسترسه: ساخت پروژه، ارسال پیام به ایجنت، خواندن فایل‌ها، دیدن diff،
انتشار (deploy)، فعال‌سازی دیتابیس، افزودن کانکتور (Stripe / Supabase / Resend / …)، آمار بازدید،
و تنظیم Knowledge (دستورهای دائمی پروژه).

---

## ۲. کارهایی که **تو** باید انجام بدی

فقط ۴ مورد. بقیه‌اش از همین‌جا انجام می‌شه.

### الف) کردیت‌ها را چک کن — مهم‌ترین مورد
پلن Free محدودیت پیام روزانه/ماهانه داره و **هر پیامی که از اینجا به Lovable می‌ره کردیت مصرف می‌کنه**.
- برو به `Settings → Plans & Billing` در سایت Lovable.
- ببین چند پیام باقی مونده.
- اگر کم بود، بگو تا پیام‌ها را **تجمیع** کنم (چند تغییر در یک پیام) به‌جای پیام‌های تک‌تک.

### ب) پروژه را در مرورگر باز کن و تأیید کن
- **ادیتور:** https://lovable.dev/projects/bbc8f08a-20d6-4aaa-bcb8-0a358f903954
- **پیش‌نمایش زنده:** https://id-preview--bbc8f08a-20d6-4aaa-bcb8-0a358f903954.lovable.app

نگاه کن ببین ظاهر سایت مطابق انتظارته یا نه. **بازخوردت را با جزئیات بده** — مثلاً
«فونت هدر کوچیکه» یا «رنگ دکمه‌ها را آبی کن» — نه «قشنگ نیست».

### ج) اگر می‌خوای کد روی گیت‌هاب باشه
در ادیتور Lovable: `GitHub → Connect to GitHub`. بعدش هر تغییری خودکار کامیت می‌شه و
من هم می‌تونم مستقیم روی همون ریپو کار کنم. برای پروژه‌های جدی **قویاً توصیه می‌شه** —
بدون این، کد فقط داخل Lovable می‌مونه.

### د) برای انتشار عمومی (اختیاری)
بگو تا `deploy` کنم؛ یک آدرس `*.lovable.app` می‌گیری که هر کسی می‌تونه ببینه.
دامنه‌ی اختصاصی (مثل `damdari-mehr.ir`) نیاز به پلن پولی داره.

---

## ۳. پلن دقیق برای مدل Sonnet 5

منظور از «پلن» اینجا یعنی: **روش پرامپت‌نویسی که خروجی درست بده و کردیت هدر نده.**
Sonnet 5 در Lovable وقتی خوب کار می‌کنه که ورودی‌اش ساختاریافته باشه.

### ۳.۱ قانون طلایی: پرامپت را در ۵ بخش بنویس

```
۱. CONTEXT   — این چیه و برای کیه (۱ خط)
۲. GOAL      — دقیقاً چه چیزی باید تغییر کنه (۱ تا ۳ خط)
۳. SPEC      — لیست دقیق و شماره‌دار از اجزا، متن‌ها، رفتارها
۴. CONSTRAINTS — چیزهایی که نباید بشکنه (RTL، فونت، پالت رنگ، ساختار فایل)
۵. DONE WHEN — معیار پذیرش، قابل تست با چشم
```

بخش ۵ مهم‌ترینه و اکثر آدم‌ها ننویسنش. بدون اون، مدل خودش تصمیم می‌گیره کِی «تمام» شده.

### ۳.۲ الگوی آماده (کپی کن و پر کن)

```
CONTEXT: سایت معرفی «دامداری مهر»، فارسی و RTL.

GOAL: صفحه‌ی محصولات را به حالت جست‌وجوپذیر تبدیل کن.

SPEC:
1. یک input جست‌وجو بالای گرید، با placeholder «جست‌وجوی محصول…» و آیکون ذره‌بین سمت راست.
2. فیلتر بر اساس نام و توضیح محصول، بدون حساسیت به حروف، با debounce ۳۰۰ms.
3. جست‌وجو و تب دسته‌بندی باید هم‌زمان اعمال بشن (AND).
4. حالت خالی: آیکون + متن «محصولی یافت نشد» + دکمه «پاک کردن فیلترها».

CONSTRAINTS:
- چیدمان RTL نشکنه.
- از توکن‌های رنگی موجود استفاده کن، هیچ hex جدیدی هاردکد نکن.
- هیچ پکیج جدیدی نصب نکن.
- به صفحات دیگر دست نزن.

DONE WHEN: با تایپ «شیر» فقط محصولات لبنی نمایش داده بشن، و با تایپ «xyz» حالت خالی بیاد.
```

### ۳.۳ چهار قانون عملیاتی

**۱) هر پیام = یک هدف.**
«صفحه بلاگ بساز + رنگ‌ها را عوض کن + فرم را درست کن» ⟵ غلط.
مدل یکی را خوب انجام می‌ده و بقیه را نصفه. سه پیام جدا بفرست.

**۲) برای کارهای بزرگ اول `plan_mode`.**
قبل از هر تغییر معماری (افزودن دیتابیس، احراز هویت، درگاه پرداخت)، اول در حالت پلن
بپرس چطور می‌خواد انجام بده. کد نمی‌زنه، فقط نقشه می‌ده. بعد تأیید کن.
این کار جلوی بازنویسی‌های پرهزینه را می‌گیره.

**۳) قوانین دائمی را در Knowledge بذار، نه در هر پیام.**
چیزهایی مثل «همیشه RTL»، «فونت Vazirmatn»، «هیچ متن انگلیسی در UI» را یک‌بار در
Project Knowledge می‌نویسیم. بعد از اون در هر پیام رعایت می‌شه و لازم نیست تکرار کنی.

**۴) وقتی باگ داری، علامت را بده نه راه‌حل را.**
❌ «`useEffect` را عوض کن»
✅ «در موبایل، منوی همبرگری باز می‌شه ولی با کلیک روی لینک بسته نمی‌شه و صفحه عوض نمی‌شه.»
مدل کد را می‌خونه و ریشه را پیدا می‌کنه؛ حدس تو ممکنه غلط باشه و مسیر را منحرف کنه.

### ۳.۴ ترتیب پیشنهادی مراحل

| مرحله | کار | حالت |
|---|---|---|
| ۱ | ساخت اسکلت و همه‌ی صفحات با محتوای واقعی | عادی |
| ۲ | تنظیم Knowledge پروژه (قوانین دائمی) | — |
| ۳ | اصلاح ظاهر و ریسپانسیو موبایل | عادی |
| ۴ | افزودن تعامل‌ها (فیلتر، جست‌وجو، لایت‌باکس) | عادی |
| ۵ | تصمیم درباره‌ی بک‌اند | `plan_mode` |
| ۶ | اتصال دیتابیس / فرم واقعی | عادی |
| ۷ | سئو، متا تگ، فاوآیکون، سرعت | عادی |
| ۸ | انتشار | `deploy` |

---

## ۴. محدودیت‌هایی که بهتره از الان بدونی

- **تایم‌اوت ۶۰ ثانیه‌ای MCP.** ساخت پروژه معمولاً بیشتر طول می‌کشه، پس اولین فراخوانی
  خطای تایم‌اوت می‌ده — ولی پروژه در پس‌زمینه ساخته می‌شه. این خطا را نادیده بگیر؛
  من وضعیت را جداگانه چک می‌کنم.
- **Lovable مالک کده، نه من.** من از طریق MCP بهش پیام می‌دم؛ کد را خودش می‌نویسه.
  پس نتیجه ۱۰۰٪ قابل پیش‌بینی نیست و باید بعد از هر مرحله چشمی بررسی بشه.
- **استک ثابته:** React + TypeScript + Tailwind + shadcn/ui. انتخاب فریم‌ورک نداریم.
- **پلن Free:** دامنه اختصاصی و بعضی قابلیت‌های تیمی نداره.

---

## ۵. برای پروژه‌های جدی بعدی

اگر این تست جواب داد، برای کار واقعی این سه تا را از روز اول انجام بده:

1. **گیت‌هاب را وصل کن** — تا کد جایی امن باشه و نسخه‌بندی داشته باشه.
2. **Knowledge پروژه را کامل بنویس** — قوانین برند، رنگ، لحن متن، کتابخانه‌های مجاز.
3. **پلن پولی بگیر** — کردیت Free برای یک پروژه‌ی واقعی کافی نیست.
